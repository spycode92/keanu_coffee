<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.keanu_coffee.admin.mapper.SystemNotificationMapper">
	<!-- 시스템 알림 -->
	<select id="selectNotificationList" resultType="SystemLogDTO">
		SELECT *
			FROM SYSTEM_LOG
		<where>
		<if test="searchType != ''">
			${searchType} LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		</where>
		<if test="orderKey != ''">
			ORDER BY ${orderKey} ${orderMethod}			
		</if>
		LIMIT
			#{startRow}
			, #{listLimit}
	</select>
	
	<!-- 목록 갯수 -->
	<select id="selectNotificationCount" resultType="int">
		SELECT count(*)
		FROM SYSTEM_LOG
		<where>
		<if test="searchType != ''">
			${searchType} LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>

		</where>
	</select>
	
	
	
	
	
</mapper>