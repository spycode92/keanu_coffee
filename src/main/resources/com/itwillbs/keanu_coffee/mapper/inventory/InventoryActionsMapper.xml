<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.keanu_coffee.inventory.mapper.InventoryActionsMapper">
	
	<insert id="insertWarehouse" >
	
		<foreach collection="locationList" item="item" separator=";">
			INSERT INTO 
				WAREHOUSE_LOCATIONS (rack, bay, level_position, width, depth, height)
			VALUES (
	            #{item.rack},
	            #{item.bay},
	            #{item.levelPosition},
	            #{item.width},
	            #{item.depth},
	            #{item.height}

			)
		</foreach>
		
	</insert>






















</mapper>